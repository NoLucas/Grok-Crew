# Grok Crew

<p align="center">
  <img src="public/hero.png" alt="Grok Crew with Grok Bot — 本地影像工作台" width="100%" />
</p>

**v1.0.18** · with Grok Bot · 现在免费

不必再通宵自己剪 Shorts。
接上已经在用的 **Grok Bot** 或 Agent，成品进 **这台电脑的文件夹**。
没有账号，没有银行卡，没有按条计费。下载后打开即可。

[下载 Windows](https://github.com/NoLucas/Grok-Crew/releases/tag/v1.0.1) · [한국어](README.ko.md) · [English](README.md) · [日本語](README.ja.md)

---

## 给谁用

- **懒得自己剪 Shorts 的人。** 不用从零搭时间线。写下想做的事，贴进机器人窗口，在这里保存。
- **现在免费。** 没有登录。不是订阅。也没有按条积分。
- **文件留在这台电脑。** 不是云端剪辑器。

这个窗口不是“我们来生成视频”的订阅。它是接上你已有机器人的程序。

---

## 给机器人和 Agent 的内置技能

连接时，座位已经带好技能。不必自己写策划、抓取、剪辑提示词。

| 座位 | 基础技能 | 一并带上的额外技能 |
|---|---|---|
| **Grok Bot / Agent 策划** | `planner` — 把今天的话写成剪辑计划 | `edit-plan` — 给抓取和剪辑读的短计划 |
| **Grok Bot / Agent 抓取** | `scraper` — 只收策划写的直接文件地址 | `public-pick` — curl 后再 CopyFromBox 到邀请文件夹，不搜索 |
| **Grok Bot / Agent 剪辑** | `editor` — 只剪 Windows 素材箱 | `cut-to-plan` — 守场景顺序和目标市场的节奏 |

原文在 [`public/bot-skills/`](public/bot-skills/)。复制接上用的文字时，该座位的技能一起走。这个程序不抓网站。

---

## 怎么用

1. 打开 `GrokCrew-Windows.exe`。安装程序先问要留下哪个 TTS，只下载那一个，然后再复制程序。不选就是 Kokoro-82M。
2. 第一次打开先选语言。安装程序已经留下声音时，不再问 TTS。
3. 在 **连接** 把接上用的文字复制到机器人窗口。只复制不算已连接。机器人只发 `GROK_CREW_OK` 就停下，说明工作还没送到。把 **开始** 里的文字贴到那个窗口，它才会做那个位子的工作。不要在聊天里做 Routine、预约作业或 `keep`。**断开连接**会发送 `disconnected`。
4. 在 **开始** 写下想做的事，按 **开始制作**。同一台 Windows 上已签到的 Grok 位子会自己读待办邀请。这个窗口等待。**组员看板**对话按位子把开工和转交的一行堆成聊天。
5. 预览出来后 **存到这台电脑**。发布是下一步，而且只有他们想发时才做。

接不上时，**连接** 会写怎么修。不要先开开始。

---

## 还没进安装文件的修复

- 1.0.18 只复制连接文字就会签到，并把开始邀请藏起来。这个分支把它改回去。
- 只复制不算已连接。只有短的 `GROK_CREW_OK` 才算签到。把开始里的文字贴到那个窗口才会做事。

## v1.0.18 改了什么

- **连接**里没有再贴 `GROK_CREW_OK` 的格子。复制连接文字后，这张书桌会签到。
- 机器人的第一句回复是 `GROK_CREW_OK`，然后只做那个位子的工作。策划、采集、剪辑不再共用同一段「停下」文字。

完整列表见 [CHANGELOG.md](CHANGELOG.md) 和 [1.0.18 发布说明](docs/RELEASE_NOTES.v1.0.18.md)。客人用的网盘文件不变。已安装的书桌看 Grok-crew-test 的 v1.0.18。

## v1.0.17 改了什么

- 机器人只发 `GROK_CREW_OK` 然后停下。不要在聊天里做 Routine 或 `keep`。把那一行贴到连接，这张书桌会签到。
- Linux 上的 Grok 不要说没有窗口或没有 7214。

1.0.17 列表见 [1.0.17 发布说明](docs/RELEASE_NOTES.v1.0.17.md)。

## v1.0.16 改了什么

- 每分钟位子确认由 Windows 上的 `keep` 来做，不是聊天预约。**断开连接**会发送 `disconnected`，`keep` 看到后停下。
- **组员看板**对话按位子把开工和转交的一行堆成聊天。占位确认不进对话。

1.0.16 列表见 [1.0.16 发布说明](docs/RELEASE_NOTES.v1.0.16.md)。

## v1.0.15 改了什么

- 同一台电脑已签到的 Grok 位子会在开始制作后读取待办邀请。那些位子不再显示再复制。
- 左侧预览里的视频可以直接播放。点名字仍会打开该作业。
- 英语、中文、日语的连接文字和 TTS 保留句保持该语言。首页也改成开始，不再写自动。

1.0.15 列表见 [1.0.15 发布说明](docs/RELEASE_NOTES.v1.0.15.md)。

## v1.0.14 改了什么

- 只要机器人已连接并且写了要做的片子，就可以开始。只有打开的选项才会检查素材。
- 左侧是文件夹和文件预览。下面的说明和不用的命令条已经去掉。
- 收集地址也会拒绝 IPv6 套出来的内网。书桌只跟随本机回环工作室地址。

1.0.14 列表见 [1.0.14 发布说明](docs/RELEASE_NOTES.v1.0.14.md)。

## v1.0.13 改了什么

- TTS 试听只用目录里的说话人，也不再返回这台电脑的绝对路径。
- 收集地址只收公开文件网址。`/media` 只给预览文件。

1.0.13 列表见 [1.0.13 发布说明](docs/RELEASE_NOTES.v1.0.13.md)。

## v1.0.12 改了什么

- TTS 语种只显示已安装模型会说的语言。Kokoro-82M 没有韩语语言包。
- 试听是 Kokoro-82M 的 WAV。

1.0.12 列表见 [1.0.12 发布说明](docs/RELEASE_NOTES.v1.0.12.md)。

## v1.0.11 改了什么

- 自动收集只收公开文件地址。搜索词不会开工。
- 选自己的文件时，不再因为工作区相对路径 `inputs\...` 报错。

1.0.11 列表见 [1.0.11 发布说明](docs/RELEASE_NOTES.v1.0.11.md)。

## v1.0.10 改了什么

- 这台电脑已经有选中的 TTS 时，安装程序跳过下载并继续。声音下载失败也不会停住程序安装。

1.0.10 列表见 [1.0.10 发布说明](docs/RELEASE_NOTES.v1.0.10.md)。

## v1.0.9 改了什么

- 安装程序又能收下 Kokoro-82M。现在下载 Hugging Face 上真实的权重文件。1.0.8 用了不存在的文件名，所以失败。

1.0.9 列表见 [1.0.9 发布说明](docs/RELEASE_NOTES.v1.0.9.md)。

## v1.0.8 改了什么

- **隐藏和退出不一样。** **和昨天一样**一次复制三个座位的接上文字。**连接**也有组员看板。7214 和蓝屏保护说明更清楚。

1.0.8 列表见 [1.0.8 发布说明](docs/RELEASE_NOTES.v1.0.8.md)。

## v1.0.7 改了什么

- **退出**会关掉 `grok-crew-studio.exe` 和它的子进程。1.0.6 可能把它留在任务管理器里。

1.0.7 列表见 [1.0.7 发布说明](docs/RELEASE_NOTES.v1.0.7.md)。

## v1.0.6 改了什么

- 安装程序先选 TTS，再复制程序。只打开 exe 不会开始下载。第一次打开是语言。自动等待的**组员看板**只显示真实留下的一行（`detail.note`）。连接技能只对应一个国家。

完整列表见 [CHANGELOG.md](CHANGELOG.md) 和 [1.0.6 发布说明](docs/RELEASE_NOTES.v1.0.6.md)。

## v1.0.5 改了什么

- 自动等待用座位名和已有 heartbeat 写现在在做什么，不假装“已读”。Grok 邀请改为每分钟 `still_here`，工作变了就发 `plan_*` / `collect_*` / `cut_*`。已接上的位子断开时会响连接断开警报。

1.0.5 列表见 [1.0.5 发布说明](docs/RELEASE_NOTES.v1.0.5.md)。

## v1.0.4 改了什么

- 窗口开着时监听 `127.0.0.1:7214`，各国一样。同一台 Windows 上的 PowerShell 签到不再需要聊天令牌。没有 7214 时机器人只回一行就停。

1.0.4 列表见 [1.0.4 发布说明](docs/RELEASE_NOTES.v1.0.4.md)。

## v1.0.3 改了什么

- 在 **连接**，Grok Bot 应在已登记的 Windows 上批准后签到。没到的话，把机器人回的 `GROK_CREW_OK` 贴到这里。只复制不算已连接。

1.0.3 列表见 [1.0.3 发布说明](docs/RELEASE_NOTES.v1.0.3.md)。

## v1.0.2 改了什么

- 第一次打开仍会问这台电脑要装哪个 TTS。那个窗口现在铺满书桌，不再挤在侧栏里。不选就用 Kokoro-82M。安装仍是一键。

1.0.2 列表见 [1.0.2 发布说明](docs/RELEASE_NOTES.v1.0.2.md)。

## v1.0.1 改了什么

- 第一次打开会问这台电脑要装哪个 TTS。不选就用 Kokoro-82M。安装仍是一键。
- 连接灯只有 **已连接**（绿）和 **未连接**（灰）。只复制接上用的文字不会变绿。
- 退出先警告。确认后断开 Grok Bot 和 Agent。只关窗口会藏到托盘，连接还在。

1.0.1 列表见 [1.0.1 发布说明](docs/RELEASE_NOTES.v1.0.1.md)。

## v1.0.0 改了什么

以前的 Grok-Crew（0.2.1）更像浏览器工作台。现在的 1.0.0 是 **这台电脑上的窗口**。

- 连接、自动、设置、编辑、导出同一套写法：一件事，其余是芯片。
- **Grok Bot** 和 **Agent** 按策划、抓取、剪辑接上。内置技能一起走。
- 只复制接上用的文字。不用把回复再贴回这个窗口。不把 `127.0.0.1` 给机器人看。
- 字幕、配音、TTS 默认全关，打开才跑。第一次打开不挡在选声页面。
- 成品留在这台电脑。GitHub 收件箱可有可无。
- 配对码用密码学随机数。GitHub 令牌出错时不再把令牌写回去。

1.0.0 列表见 [CHANGELOG.md](CHANGELOG.md) 和 [1.0.0 发布说明](docs/RELEASE_NOTES.v1.0.md)。

---

## 屏幕上有什么

每一栏只做 **一件事**。其余工具在芯片后面，问了才开。

| 栏 | 做什么 |
|---|---|
| **连接** | 接上或拆掉机器人。本机使用、其他窗口、不接机器人直接打开影片都收着。 |
| **自动** | 一栏提示。芯片：画面 / 要发到哪 / 声音。制作之后：等待 → 预览 → 保存。 |
| **设置** | 形态、长度、声音、节奏。这里的按钮是 **只保存设置**。 |
| **编辑** | 时间线和预览在前。收件文件和代理预览收着。 |
| **导出** | 先存到这台电脑。发布、交换、记录是芯片。 |
| **高级规格** | 标题和目标。风格、来源、收集、文件是芯片。只有保存之后才打开发件/收件。 |

左边 **版本记录**、右边 GitHub / 制作器默认收着，打开才看见。

---

## 这不是什么

- 云端剪辑，或在浏览器里剪的网站
- “我们来生成视频”的订阅
- 自动发到 Instagram / TikTok
- 去刮已登录的社交账号
- 把 `127.0.0.1` 给机器人看

---

## 窗口下面用了什么

客人可以不看。打开安装包即可。

| 工作 | 用什么 |
|---|---|
| 窗口 | Electron。这台电脑上的程序。 |
| 界面 | React、TypeScript。连接、自动、设置、编辑、导出。 |
| 剪辑 | 这台电脑上的 Python 制作器（Local Studio）。只和这个窗口说话。 |
| 存储 | 这台电脑上的 SQLite。没有账号服务器。 |
| 编码 | 保存时在这台电脑跑 ffmpeg、MoviePy。 |
| 字幕 | 只有打开字幕时才用 whisper.cpp。 |
| 声音 | 只有打开 TTS 时才用选中的 **一个** 引擎。 |
| 机器人技能 | `public/bot-skills/` 里的 Markdown，连接时贴上。 |

语音模型不打进安装包。客人打开该功能后才下载。

---

## 打开这个仓库

公开源码：[NoLucas/Grok-Crew](https://github.com/NoLucas/Grok-Crew)

```bash
git clone https://github.com/NoLucas/Grok-Crew.git
cd Grok-Crew
npm install
npm run local
```

客人窗口是 `npm run desktop`。只想在浏览器里看桌面，运行 `npm run dev` 再打开它打印的地址。

完整安装说明：[docs/BUILD.ko.md](docs/BUILD.ko.md)。

---

## 许可

[BUSL-1.1](LICENSE)。要改代码，先读 [CONTRIBUTING.md](CONTRIBUTING.md)。
