# Local xAI bridge. Never commit or share the real values.
XAI_API_KEY=
# Optional. The xAI API's "latest" alias is used when blank.
XAI_MODEL=
# Optional. When set, POST /api/reel-jobs/plan requires Authorization: Bearer <value>.
REEL_FORGE_LOCAL_TOKEN=
